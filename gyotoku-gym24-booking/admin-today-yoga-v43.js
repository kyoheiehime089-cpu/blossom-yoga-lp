(()=>{const $=q=>document.querySelector(q),p=n=>String(n).padStart(2,'0'),dkey=d=>d.getFullYear()+'-'+p(d.getMonth()+1)+'-'+p(d.getDate()),fmt=m=>{m=Number(m);return m===1440?'24:00':p(Math.floor(m/60))+':'+p(m%60)},esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])),label=s=>String(s||'').trim()||'未入力';function end(x){return Number(x.display_end_minute??x.end_minute)}function show(){const box=$('#todayReservations');if(!box||!window.snap)return;const today=dkey(new Date());const list=(window.snap.external_blocks||[]).filter(x=>x.date===today).sort((a,b)=>Number(a.start_minute)-Number(b.start_minute));const sig=list.map(x=>`${x.id}:${x.start_minute}:${end(x)}:${x.member_name||''}:${x.note||''}`).join('|');let old=box.querySelector('[data-today-yoga-block]');if(old&&old.dataset.sig===sig)return;if(old)old.remove();if(!list.length)return;const wrap=document.createElement('div');wrap.dataset.todayYogaBlock='1';wrap.dataset.sig=sig;wrap.innerHTML='<h3>今日のヨガ個別予約</h3>'+list.map(x=>`<article class="res yoga-private"><h3>${fmt(x.start_minute)}〜${fmt(end(x))}　ヨガ個別予約</h3><p>会員名：${esc(label(x.member_name))}</p>${x.note?`<p>メモ：${esc(x.note)}</p>`:''}<button class="danger" data-yoga-delete="${esc(x.id)}">削除</button></article>`).join('');box.appendChild(wrap)}window.addEventListener('DOMContentLoaded',()=>{setTimeout(show,700);setInterval(show,1500)});})();